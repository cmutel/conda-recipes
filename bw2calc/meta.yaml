{% set name = "bw2calc" %}
{% set version = "2.0.dev8" %}
{% set sha256 = "2287fe45812af481fea6c0fc99248125775ee5f23ad2ba0f589d66d4bbd01f49" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt
  ignore_run_exports:
    - python_abi

requirements:
  build:
    - python >=3.9
    - setuptools

  run:
    - python
    - setuptools
    - numpy >=1.20
    - scipy
    - stats_arrays
    - bw_processing >=0.8
    - matrix_utils >=0.2.5

test:
  requires:
    - pytest
    - bw2data >=4.0.dev13

  imports:
    - {{ name }}

about:
  home: https://brightway.dev/
  license: BSD 3-Clause
  license_file: LICENSE.txt
  summary: 'The calculation engine for the Brightway2 life cycle assessment framework'
  license_family: BSD
  doc_url: https://docs.brightway.dev
  dev_url: https://github.com/brightway-lca/brightway2-calc/
