{% set name = "bw2calc" %}
{% set version = "2.0.dev7" %}
{% set sha256 = "332f428cadcad78b699301eefb7e154730135809f4963d081447899ec792ded6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt
  ignore_run_exports:
    - python_abi

requirements:
  build:
    - python >=3.9
    - setuptools

  run:
    - python
    - setuptools
    - numpy >=1.20
    - scipy
    - stats_arrays
    - bw_processing >=0.8
    - matrix_utils >=0.2.5

test:
  requires:
    - pytest
    - bw2data >=4.0.dev13

  imports:
    - {{ name }}

about:
  home: https://brightway.dev/
  license: BSD 3-Clause
  license_file: LICENSE.txt
  summary: 'The calculation engine for the Brightway2 life cycle assessment framework'
  license_family: BSD
  doc_url: https://docs.brightway.dev
  dev_url: https://github.com/brightway-lca/brightway2-calc/
